def smallerNumbers(arr, N):
    for i in range(N):
        count = 0
        for j in range(N):
            if (arr[j] < arr[i]):
                count += 1
        print(count, end=" ")
if __name__ == "__main__":
 
    arr = [6,5,4,8]
    N = len(arr)
 
    smallerNumbers(arr, N)
